# 正式环境必填：站点根 URL（https 开头，无尾斜杠）。用于 canonical、og:url、WebSite JSON-LD，避免搜索引擎把预览/测试域名当规范地址。
# 须与用户实际访问的地址一致（例如已绑定自定义域时用主域，不要与真实访问域名不一致）。
# 品牌名 Roamwise 与域名可不同；当前主站示例：
# VITE_SITE_URL=https://helloroamly.com
# 若 www 为主：
# VITE_SITE_URL=https://www.helloroamly.com

# 可选：社交分享默认图（完整 https，建议 1200×630）。未设置时，用站点 favicon.svg 的绝对地址作为 og:image。
# VITE_OG_IMAGE=https://www.example.com/og-card.png

# —— AI 客服（真实 API）——
# 未设置时使用本地模拟回复。设置后 POST JSON { "message": "用户输入" }，建议返回 { "reply": "..." }。
# 限流/配额：HTTP 429/503，或 JSON quotaExceeded / serviceBusy / error 为 rate_limit|quota；响应头 X-RateLimit-Remaining 为 0 时前端提示「服务繁忙」并回退本地次数。
# VITE_AI_API_URL=https://your-api.example.com/chat
# VITE_AI_CHAT_ENABLED=true
# VITE_AI_FREE_QUOTA=10

# —— Giscus（GitHub Discussions 评论）——
# 在 https://giscus.app/zh-CN 生成配置；仓库需开启 Discussions 并安装 Giscus App。
# 三项必填才会加载评论区：仓库、repo-id、category-id。
# VITE_GISCUS_REPO=你的用户名/你的仓库名
# VITE_GISCUS_REPO_ID=R_xxxxx
# VITE_GISCUS_CATEGORY=Comments
# VITE_GISCUS_CATEGORY_ID=DIC_xxxxx
# 可选：pathname（默认）| specific | title 等，见 giscus 文档
# VITE_GISCUS_MAPPING=pathname

# 可选：攻略页「去 GitHub 讨论区留言」链接
# VITE_GITHUB_DISCUSSIONS_URL=https://github.com/你的用户名/你的仓库名/discussions

# —— 合规与页脚（面向中国大陆用户时建议配置）——
# 工信部备案号全文，例如：京ICP备12345678号-1
# VITE_ICP_FILING=京ICP备12345678号-1

# 友情链接 JSON 数组，示例：[{"label":"示例站","url":"https://example.com"}]
# VITE_FRIENDLY_LINKS_JSON=[{"label":"GitHub","url":"https://github.com/你的用户名/你的仓库名"}]

# —— 会员页「开通会员」按钮（可选）——
# 未设置时按钮指向站内 /about。设置后为新标签页打开的外部开通/支付页。
# VITE_MEMBERSHIP_URL=https://your-payment-or-campaign.example.com/join
